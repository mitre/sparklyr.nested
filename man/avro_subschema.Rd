% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/avro.R
\name{avro_subschema}
\alias{avro_subschema}
\title{Subset an Avro schema to a set of fields}
\usage{
avro_subschema(
  schema,
  fields,
  keep_docs = FALSE,
  keep_namespace = FALSE,
  keep_java_class = keep_namespace,
  keep_default = FALSE,
  ...
)
}
\arguments{
\item{schema}{Character or list. The Avro schema to subset (JSON string or
parsed list).}

\item{fields}{List or character. The fields to keep. If a list, use nested
  structure mirroring the schema; if a character vector, use dot or dollar
  notation for nested paths.

  List form: \code{list("primary_key", "points" = list("latitude", "longitude", "altitude"))}

  Character form: \code{c("primary_key", "points.latitude", "points.longitude", "points.altitude")}
  or \code{c("primary_key", "points$latitude", "points$longitude", "points$altitude")}
  (dot and dollar are equivalent).}

\item{keep_docs}{Logical. If \code{FALSE} (default), remove \code{doc} entries from the schema.}

\item{keep_namespace}{Logical. If \code{FALSE} (default), remove \code{namespace} entries.}

\item{keep_java_class}{Logical. If \code{FALSE} (default), remove \code{java-class} entries.
Defaults to \code{keep_namespace}.}

\item{keep_default}{Logical. If \code{FALSE} (default), remove \code{default} entries from fields.}

\item{...}{Arguments passed to \code{\link[jsonlite]{toJSON}}}
}
\value{
A JSON string (subsetted schema).
}
\description{
Returns a simplified Avro JSON schema containing only the specified fields.
Nested fields (e.g. \code{points.latitude} in a \code{points} array of records)
are supported.
}
\examples{
\dontrun{
schema <- '{"type":"record","name":"X","fields":[{"name":"a","type":"string"},{"name":"b","type":"int"}]}'
avro_subschema(schema, "a")
avro_subschema(schema, list("a", "b"))
}

}
